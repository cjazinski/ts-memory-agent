# GitHub Models API Token (primary - recommended)
# Get a token with 'models:read' permission from GitHub Settings > Developer Settings > Personal access tokens
GITHUB_TOKEN=your-github-token
# Or use this alternative env var name:
# GITHUB_MODELS_TOKEN=your-github-token

# Model Configuration (optional - defaults shown)
CHAT_MODEL=openai/gpt-4o-mini
EMBEDDING_MODEL=openai/text-embedding-3-small

# OpenAI API Key (fallback - used if GITHUB_TOKEN not available)
# OPENAI_API_KEY=your-openai-api-key

# Project ID is passed as a command line argument:
#   npm run dev <project-id>
#   npx tsx src/index.ts <project-id>
# For the API server, project ID is passed in each request body

# Redis Configuration (optional - will use SQLite if not available)
REDIS_URL=redis://localhost:6379
# Or use individual settings:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# SQLite fallback path (used when Redis is not available)
SQLITE_PATH=./data/project-memory.db

# API Server Port
PORT=3000
